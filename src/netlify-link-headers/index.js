import { Promise } from 'bluebird'
import { promises as fs } from 'fs'
import path from 'path'
import { options } from '../util'

export function onCreateTree (tree, { setActivityStatus }) {

}

export function onNode (node, { setActivityStatus, deleteNote }) {

}

export function onSerializeTree (tree, { setActivityStatus }) {

}

export function onWrite (html, { setActivityStatus, setHtml }) {

}
